import styled from 'styled-components';

export const MainContainer = styled.div`
  /* height: 100vh; 전체 뷰 높이 */
  width: auto;
  margin: 0 auto;
  padding-left: 38px;
  display: flex;
  height: 100vh; /* 화면 높이를 기준으로 고정 */
  overflow: hidden; /* 부모는 스크롤 숨김 */
  /* overflow: visible; */
`;
